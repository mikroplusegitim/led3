[build]
  publish = "frontend/dist"
  command = "cd frontend && npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# Production environment variables buraya eklenecek
# Netlify Dashboard > Site settings > Build & deploy > Environment variables
